
const first = document.getElementById('firstNumber').innerText
const second = document.getElementById('secondNumber').innerText

function addition(first, second) {
  return parseInt(first) + parseInt(second)
}

function multiplication(first,socond) {

    return parseInt(first) * parseInt(second)
}

const result = multiplication("20", "42")

addition(firstN, secondN)

document.getElementById("result").innerText = result
